import type { GameDefinition } from "../engine/game-engine";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const games: GameDefinition<any, any, any>[] = [
  // hokm,   <- one line per game
];
